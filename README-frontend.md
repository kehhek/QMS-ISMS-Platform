Frontend stub (React)

To run the frontend locally:

```bash
cd frontend
npm install
npm start
```

The stub expects the Django backend at the same host under `/api/` paths. Use token from `/api/accounts/token/` and paste into the token input to view users/groups.
